-- tokyoppuccin — unsuffixed entry point.
-- `:colorscheme tokyoppuccin` loads whichever style setup{} configured (default
-- "storm"), so muscle memory and `install = { colorscheme = { "tokyoppuccin" } }`
-- both work. Use tokyoppuccin-<style> to pin a variant regardless of config.
require("tokyoppuccin").load()
